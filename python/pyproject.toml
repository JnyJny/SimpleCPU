[tool.poetry]
name = "simplecpu"
version = "0.1.0"
description = ""
authors = ["JnyJny <erik.oshaughnessy@gmail.com>"]
readme = "README.md"

[tool.poetry.scripts]
prun="cpusim.__main__:loader"
pasm="cpusim.__main__:asm"
pdis="cpusim.__main__:dis"

[tool.poetry.dependencies]
python = "^3.11"
loguru = "^0.7.2"
typer = "^0.9.0"

[tool.poetry.group.dev.dependencies]
pytest = "^7.4.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
